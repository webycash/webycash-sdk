[package]
name = "webycash-sdk-example"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
webylib = { version = "0.2", features = ["ffi"] }
tokio = { version = "1", features = ["full"] }
